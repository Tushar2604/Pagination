# Leave empty to use same-origin requests via Next.js proxy (recommended for local dev)
# NEXT_PUBLIC_API_BASE_URL=

# Backend URL used by Next.js rewrites (server-side only)
BACKEND_URL=http://localhost:3000

# Optional: used for server-side fetches when API base URL is empty
NEXT_PUBLIC_SITE_URL=http://localhost:3001
